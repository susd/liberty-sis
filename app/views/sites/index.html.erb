<div class="page-header">
  <h1>Sites</h1>
</div>

<div class="list-group">
  <% @sites.each do |site| %>
  <%= link_to site.name, site_classrooms_path(site), class: 'list-group-item' %>
  <% end %>
</div>
